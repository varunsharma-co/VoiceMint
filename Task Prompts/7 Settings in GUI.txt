Let's work on the next task: expanding our CustomTkinter UI to include a functional Settings panel that connects to our `config.py` file.

First, I want to overhaul the look of our GUI. Currently, it is extremely crude and raw. Please explicitly use modern elements and widgets from CustomTkinter (like segmented buttons, modern switches, clean frames, tabviews, and rounded corners) to make it highly aesthetically pleasing, modern-looking, and far better looking. Make it clean, beautiful, and highly polished before adding these new elements.

**The Feature Request:**
I want to add a "Settings" toggle/button to the main UI. When clicked, it should reveal a categorized settings panel. Because there are over 10 settings, I do not want a massive list. They must be categorized into clean, collapsible sections or separate tabs.

Here is the mapping of the UI settings to the exact variables in `config.py`:

**1. Application Settings**
* **STT Provider:** Dropdown menu mapping to `STTProvider` Enum (`soniox`, `deepgram`, `assemblyai`). Default is `soniox`. Updates `ACTIVE_STT_PROVIDER`.
* **Injection Method:** Dropdown menu mapping to `InjectionMethod` Enum (`uinput`, `clipboard`). Default is `uinput`. Updates `ACTIVE_INJECTION_METHOD`.
* **Silence Timeout:** Text entry box for a float value. Default is `45.0`. Updates `SILENCE_TIMEOUT_SECONDS`.

**2. Hotkey Settings**
* **Start Hotkey:** Updates `HOTKEY_START`.
* **Stop Hotkey:** Updates `HOTKEY_STOP`.
* *UI Requirement:* The config expects formats like `'<cmd>+u'`. In the GUI, please display a static label reading `Super + ` followed by a small text box where the user just enters the single character (e.g., `u` or `i`). The backend should construct the final string.

**3. Session History Settings**
* **Max History Messages:** Text entry box for an integer. Updates `MAX_HISTORY_MESSAGES`. (Default: 5)
* **History Save Interval:** Text entry box for a float (minutes). Updates `HISTORY_SAVE_INTERVAL_MINUTES`. (Default: 60.0)

**4. Clipboard Injection Tuning**
* **Ideal Flush Word Count:** Text entry box (Integer). Updates `IDEAL_FLUSH_WORD_COUNT`.
* **Failsafe Flush Multiplier:** Text entry box (Float). Updates `FAILSAFE_FLUSH_MULTIPLIER`.
* **Clipboard Restore Delay:** Text entry box (Float). Updates `CLIPBOARD_RESTORE_DELAY_SEC`.

**Architectural Guidelines:**
* **Dedicated Settings Manager:** Feel free to create a new dedicated Python file within the `ui/` folder (e.g., `settings_ui.py` or similar) to house the settings UI components and the logic linking them to the config variables. Do not bloat `floating.py` if it can be avoided.
* **Persistence:** When settings are changed in the GUI, they must actually persist across app restarts. Please implement a mechanism to update and save these values permanently (either by safely rewriting the `config.py` file, or migrating us to a `config.json`/`.env` approach if you think that is architecturally cleaner).

---

I want to rethink our configuration architecture. I do not want a config.py file that holds stale hardcoded defaults while a separate JSON file holds the real settings. I want one single source of truth that I can easily read and that the UI can safely rewrite.

The Migration Plan:

Create config.json: Move all the current hardcoded variables (STT Provider, timeouts, hotkeys, etc.) out of config.py and into a new config.json file in the root directory. This will be the single source of truth.

Refactor config.py: Rewrite config.py to act solely as a loader module. When main.py or any other file imports config, config.py should read the config.json file and expose those values. Do not change how main.py or the background workers access these variables (they should still be able to call config.SILENCE_TIMEOUT_SECONDS).

The Settings UI: The new CustomTkinter settings panel will read from and write directly to config.json.

App Restart: Changes made in the GUI do not need to hot-reload into the background threads. I am fine with restarting the app for changes to take effect. But when I change the settings, the app should prompt me to close & restart the app.

Aesthetics: As requested before, use modern CustomTkinter elements (segmented buttons, rounded frames, clean tabviews) to make the GUI highly polished. Do not touch main.py or the core background worker logic.

---

Four things:

ONE.

I really like your suggestion. I think let's I should ask the AI to completely abandon the custom TIC interlibrary and instead just focus on TIC inter.  In fact, it would be great as you suggested to give it specific guidelines to the AI on what to build and what sizes to take and whatnot.  So  a.  Actually I'm not using Ubuntu. My global font is set to Noto Sans.  So we should actually tell the  tell it.  To use Noto Sans.  And then you can also give it all the suggestions on what to do with regards to I mean, give it specific also design suggestions on what to do.  Such as you can tabulate other any colors that you think would be a good idea. But I think we should not mention colors because anyways TIC inter would not take colors.  But if we have the ability you should mention it how much padding there should be —  And all that.  So it looks slightly modern. 


TWO.

In addition .  Please also include instruction for the AI to .  Basically —  Uh, some of these variables such as the font size, and the pattern details, and all that—keep that more towards the top in the file.  So that if later on I want to personally change these values for trial, I could easily change these values that are put at the top rather than finding them throughout the file. 

THREE.

Also mentioned to the AI that if possible, if it is possible in the library that the selection for .  The DTS provider as well as injection method should be  a drop.  Down rather than selecting across the three buttons like I'm showing in the screenshot because in the future, let's say I add five providers, then I don't want to have like one row splitting across five different values. As in the screenshot with three columns in the rows, I would rather have a drop-down .  In addition, I also want on this settings page below the save  first.  Of all, the save button should be named save and exit  and.  Below .  The save button, there should be a note which would say something like note, and then say for the settings to come into effect, please click the save and exit button above.  Or you should just the button should just say save, and there should be a note below that says note for these settings to come into effect, please save above. Please click the save button, and .  Close the app, and open it again for the changes to take effect. Something like that.  And also, if we just and this is I'm asking a question to you. I'm asking what should we do? Should we just use save? Because if it is saved, then we also have to put out a like a slight drop-down or toggle showing to the user changing sampling save you can close the app now and open it again.  Or what we can do is probably have the button say save and exit, and whenever we save and exit,  the app will .  Completely terminate including from the system tray  and.  Then will have to open it manuall y.  But in that case, the note message should also be different. What do you suggest? 

FOUR. 
Think . And.  Final thing,  active.  Knows it is saying that in the current version of how it is done there is press save and restart,  wait.  The app basically shows a empty window. The screenshot of which I've shared, and it also simultaneously gives this error inside the terminal. So we also have to inform the AI of this. The error is actually below. 


(voicemint) varun@Varun-Book2-Linux:~/Desktop/Projects/Python/VoiceMint$ python main.py
===================================================
🎤 VoiceMint Starting...
===================================================
[Main] System Tray and background workers active.
Exception in Tkinter callback
Traceback (most recent call last):
  File "/usr/lib/python3.12/tkinter/__init__.py", line 1967, in __call__
    return self.func(*args)
           ^^^^^^^^^^^^^^^^
  File "/home/varun/Desktop/Projects/Python/VoiceMint/voicemint/lib/python3.12/site-packages/customtkinter/windows/widgets/ctk_button.py", line 554, in _clicked
    self._command()
  File "/home/varun/Desktop/Projects/Python/VoiceMint/ui/settings_ui.py", line 154, in _on_save
    self._prompt_restart()
  File "/home/varun/Desktop/Projects/Python/VoiceMint/ui/settings_ui.py", line 167, in _prompt_restart
    dialog.grab_set()
  File "/usr/lib/python3.12/tkinter/__init__.py", line 963, in grab_set
    self.tk.call('grab', 'set', self._w)
_tkinter.TclError: grab failed: window not viewable

---

Can you please answer my question that I have asked you in this long question and also prepare a mega prompt for me that includes all the details that I have shared with you?  And then give it to the AI so that it can work on it now.
