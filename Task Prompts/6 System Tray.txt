The next step in our process is to implement the system for notifications. So whenever we run our Python script, at the start of the day, it will create a desktop system, basically show an icon, and it'll also create a notification.

Not a notification, but it will create an icon in the system tray. And whenever we actually turn on the web sockets and voice input, basically when we connect to the API, the streaming speech-to-text API, the icon color will change.

I have created a new folder called assets, under which I have created two icons. One of them will be used when the main script is active, but we are not actively connected to the streaming speech-to-text API. And whenever we are connected, the other icon will get active.

In addition, whenever we actively connect to the streaming speech-to-text API, for example, if I press the keyboard shortcut and connect to the API and speak into my mic, in addition to the icon changing, there should also be a notification in the system. So both icon change as well as notification.

The notification text to be given is shared below. Similarly, when I turn off the voice dictation, but the app is active, another notification will be given that the voice typing app has, voice typing has stopped. The text for both the activation and deactivation is given below.

Now, note that in the Gemini file, I mentioned that we are using PyStray for the library for the notifications, but we will not use that. Instead, we will work with a native Python application, the PyGObject application. For this system notification, as well, not just notification, but for this system tray icon, I assume this is not to be installed via pip, but via sudo. So if that is the case, let me know and I will install it.

There's one thing to note that when using PyGObject for the system tray icons, I prefer the icons not be read directly from the SSD, but instead I would like that whenever I open the app, once in the morning, the two icons are put entirely into the RAM. And because they will be put entirely into the RAM, you can get the RAM address of these icons in the /dev/shm folder on the RAM. And just use those RAM icons, I mean, the RAM address for the icons to inject into the code for PyGObject.

However, we will also create a fail-safe just in case the accurate RAM address is not being able to be fetched. For these icons, then their exact location on the SSD, which is inside the assets folder, will be used in that case as a fail-safe. They will not be loaded from the RAM, but directly from the SSD.

The two icons have been saved inside /assets folder are named:
icon-off-128.png
icon-on-128.png


The nofications to send are:
<when turning on>
(in bold) VoiceMint Activated (bold off)
Listening via {dynamically fill the name of STT provider set in config}...

<when turning off>
(in bold) VoiceMint Deactivated (bold off)
Stopped Listening...

In addition, also add notifications for when if I accidentally try to activate or deactivate if the app / voice input is already active or deactivated respectively. Give an appropriate notification then. 

The asset folder also has 2 sounds that I want to be played in addition to the notification shown by Linux Mint Cinnamon.
One is start.wav
Anotehr is stop.wav

You should also put these wav files into RAM and also put a failsafe to the original files.
for these wav files, you can use aplay library to play these sounds.

For the notifications, use the native Linux notify-send.

AppIndicator Menu: AppIndicator3 strictly requires a right-click menu attached to it, otherwise the icon might not render at all on Linux Mint. Please include a very basic GTK menu with a simple "Quit/Exit" button.

RAM Cleanup on Exit: Hook into the application's exit/quit event to delete the 4 assets from /dev/shm so we don't leave ghost files in the RAM disk when the application is closed.

Please keep this in mind when writing the GTK tray logic, and ensure that any UI state changes (like swapping the icon color) are handled safely according to GTK's threading rules so we don't run into segmentation faults.
Context on Audio Playback: When you implement the aplay execution for the notification sounds, please make sure the playback is handled asynchronously/non-blockingly. We just need to ensure that triggering the .wav file doesn't freeze or delay the application's main flow while the sound is playing.

---

ENsure that if we close the app the objects in RAM are removed and there is no resourve wastage. also update @GEMINI.md based on what we did in this task.

---

-----

**Copy and paste the following:**

The next step in our process is to implement the system for notifications. So whenever we run our Python script at the start of the day, it will create an icon in the system tray. And whenever we actually turn on the web sockets and voice input—basically when we connect to the streaming speech-to-text API—the icon color will change.

I have created a new folder called assets, under which I have created two icons. One of them will be used when the main script is active, but we are not actively connected to the streaming speech-to-text API. And whenever we are connected, the other icon will get active.

In addition, whenever we actively connect to the streaming speech-to-text API (for example, if I press the keyboard shortcut, connect to the API, and speak into my mic), there should also be a desktop notification. So both the icon changes and a notification fires. Similarly, when I turn off the voice dictation, but the app is still active in the background, another notification will be given that voice typing has stopped.

In addition, also add notifications for when I accidentally try to activate or deactivate if the app / voice input is already active or deactivated respectively. Give an appropriate notification then.

The asset folder also has 2 sounds that I want to be played in addition to the notification shown by Linux Mint Cinnamon: `start.wav` and `stop.wav`. For these wav files, use the native Linux `aplay` command to play them.

Now, note that in the Gemini file, I mentioned we are using PyStray. We will **not** use that. Instead, we will work with a native PyGObject application.

**Important Package Context:** I have already installed the modern system-level packages for Linux Mint (`gir1.2-ayatanaappindicator3-0.1`) and compiled `PyGObject` in my virtual environment. Please use the modern `AyatanaAppIndicator3` for the tray icon, as the old `AppIndicator3` is deprecated on Linux Mint.

**RAM Caching:** When using PyGObject for the system tray icons and audio, I prefer the assets not be read directly from the SSD. Instead, whenever I open the app once in the morning, I want the two icons and two wav files to be copied entirely into the RAM (`/dev/shm` folder). Just use those RAM addresses to inject into PyGObject and `aplay`. However, we will also create a fail-safe. Just in case the RAM address cannot be fetched, use their exact location on the SSD (inside the `/assets` folder) as a fallback.
The asset filenames are:
`icon-off-128.png`
`icon-on-128.png`
`start.wav`
`stop.wav`

**Notifications & Text:**
Use the native Linux `notify-send` command. Keep in mind that Cinnamon natively bolds the "Summary" (the first string argument) by default, so just pass these raw strings:

\<when turning on\>
Summary: VoiceMint Activated
Body: Listening via {dynamically fill the name of STT provider set in config}...

\<when turning off\>
Summary: VoiceMint Deactivated
Body: Stopped Listening...

**Architectural Guidelines to Keep in Mind:**
Please keep these constraints in mind when writing the code, as we need this to run perfectly as a background Linux daemon:

1.  **Threading & GTK Rules:** The core application logic operates on background threads. Ensure that any UI state changes (like swapping the icon color) are handled safely according to GTK's threading rules (e.g., using `GLib.idle_add`) so we don't run into segmentation faults.
2.  **The "Two UI" Thread Clash:** `Gtk.main()` will want the main thread. Keep in mind that we also have a CustomTkinter GUI. Please rethink how we manage these two visual systems so they aren't fighting on X11. Consider a "lazy-loading" approach where the CustomTkinter UI isn't initialized until the text-formatting hotkey is pressed, or find a safe way to bridge their event loops natively so dictation doesn't freeze.
3.  **Audio Playback:** Ensure `aplay` execution is handled asynchronously/non-blockingly (e.g., using `subprocess.Popen`). Triggering the `.wav` file must not freeze or delay the application's main flow.
4.  **AppIndicator Menu:** AppIndicator strictly requires a right-click menu attached to it, otherwise the icon might not render on Mint. Please include a very basic GTK menu with a simple "Quit" button.
5.  **Safe Shutdown Sequence:** Please implement a soft shutdown sequence (like clearing a global threading Event) so background workers finish gracefully, GTK stops with `Gtk.main_quit()`, and the script unhooks the hardware naturally. Avoid using a hard `sys.exit()` in the Quit callback, as that instantly murders background daemon threads, meaning our `queue_consumer` won't finish and `main.py` will never run `close_injector()`, leaving the virtual evdev keyboard stuck in the OS.
6.  **Cleanup on Exit:** Hook into the application's soft exit event to delete the 4 assets from `/dev/shm` so we don't leave ghost files in the RAM disk. Also, in `main.py`, explicitly close and delete the `/tmp/voicemint.lock` file so we leave a zero footprint on the hard drive.

